import Edition2026Overview, { CURRENT_EDITION_METADATA } from './editions/2026/Overview';

// The site root is the current edition's overview; there is no separate landing page.
export const metadata = CURRENT_EDITION_METADATA;

export default Edition2026Overview;

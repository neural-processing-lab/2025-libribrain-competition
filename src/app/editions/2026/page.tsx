import Edition2026Overview, { CURRENT_EDITION_METADATA } from './Overview';

// Permalink for the 2026 edition. Same content as `/`, canonicalised to `/`.
export const metadata = CURRENT_EDITION_METADATA;

export default Edition2026Overview;
